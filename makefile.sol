# Make file for Dungeon Crawl (solaris)APPNAME = crawl# this file contains a list of the libraries.# it will make a variable called OBJECTS that contains all the librariesinclude makefile.objOBJECTS += liblinux.oCXX = g++DELETE = rm -fCOPY = cpGROUP = gamesMOVE = mvOS_TYPE = SOLARISCFLAGS = -Wall -D$(OS_TYPE) $(EXTRA_FLAGS)LDFLAGS = -staticMCHMOD = 2755INSTALLDIR = /opt/crawl/binLIB = -lcursesall:            $(APPNAME)install:        $(APPNAME)		$(MOVE) ${INSTALLDIR}/${APPNAME} ${INSTALLDIR}/${APPNAME}.old		$(COPY) $(APPNAME) ${INSTALLDIR}		chgrp ${GROUP} ${INSTALLDIR}/${APPNAME}		chmod ${MCHMOD} ${INSTALLDIR}/$(APPNAME)clean:		$(DELETE) *.odistclean:		$(DELETE) *.o 		$(DELETE) bones.*		$(DELETE) morgue.txt		$(DELETE) scores 		$(DELETE) $(APPNAME) 		$(DELETE) *.sav		$(DELETE) core		$(DELETE) *.0*		$(DELETE) *.lab$(APPNAME):	$(OBJECTS)		${CXX} ${LDFLAGS} $(INCLUDES) $(CFLAGS) $(OBJECTS) -o $(APPNAME) $(LIB)		strip $(APPNAME)		chgrp ${GROUP} ${APPNAME}		chmod ${MCHMOD} $(APPNAME)debug:		$(OBJECTS)		${CXX} ${LDFLAGS} $(INCLUDES) $(CFLAGS) $(OBJECTS) -o $(APPNAME) $(LIB)profile:	$(OBJECTS)		${CXX} -g -p ${LDFLAGS} $(INCLUDES) $(CFLAGS) $(OBJECTS) -o $(APPNAME) $(LIB).cc.o:		${CXX} ${CFLAGS} -c $< ${INCLUDE}.h.cc:		touch $@# DO NOT DELETE THIS LINE -- make depend depends on it.
